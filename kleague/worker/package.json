{
  "name": "kleague-odds",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute kleague-odds --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
